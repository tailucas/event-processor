version: "3.8"
services:
  app:
    build: .
    logging:
      driver: syslog
    tmpfs:
    - /tmp
    hostname: __DOCKER_HOSTNAME__
    ports:
      - "4041:4040"
      - "80:8080"
    volumes:
      - ./data:/data
      - /dev/log:/dev/log
    environment:
      FLASK_ENV: development
